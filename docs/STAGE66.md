# STAGE 66

Live chat pasted the exact session `update(t)` again (2026-09-11 23:03 CDT). Session hash `beec41f1`. Living hash `7cd81012`. `STAGE = 66`.

CPU `evaluateChatKernel` now evaluates `trefoil` with GPU mapping `KERNEL_GEOMETRY_ID` 7. `matchSessionPaste` scans pasted case labels. Session switch remains infinity | hamiltonian | triangular | torus. Klein / hopf / figure8 / trefoil stay runtime-only.

Do not promote extras into the session switch until a paste includes them.
InstancedMesh GPU attributes remain 51-impl.
