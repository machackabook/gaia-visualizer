# Stage 63 — session kernel reconfirmed + TF origin seed (2026-09-11 20:00 CDT)

Live chat pasted the exact session `update(t)` again. Session hash `beec41f1`. Living hash `7cd81012`. Klein remains runtime-only. `STAGE = 63`.

GPU transform-feedback now seeds `aPrevPos` from the first CPU `evaluateChatKernel` so frame-0 does not mix from the origin.

Do not promote klein into the session switch until a paste includes it.
InstancedMesh GPU attributes remain 51-impl.
