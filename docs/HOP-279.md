# HOP-279

Session `update(t)` re-pasted 2026-09-24 20:11 CDT. Hash `beec41f1` unchanged. Living `7cd81012`. `chatKernelLerpAlpha` shared with Hive. `main.js` now skips CPU `setMatrixAt` when `shouldSkipCpuInstanceMatrix(count)` is true (4096–16384) even if TF is off.
